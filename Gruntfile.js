'use strict';

module.exports = function (grunt) {
    require('grunt-commons')(grunt, {
        name: 'onefold-lists',
        main: 'lists',
        internalMain: 'internal'
    }).initialize({});
};
